# -----------------------------
# URLs (filled by Netlify in cloud; set explicitly in .env.local or codespace envs)
# -----------------------------
APP_URL=
NEXT_PUBLIC_APP_URL=
STAGING_URL=

# -----------------------------
# Stripe (server vs browser)
# -----------------------------
# Server-only secret key (sk_*). TEST in staging/preview; LIVE in production.
STRIPE_SECRET_KEY=
# Browser-safe publishable key (pk_*).
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# Webhook signing secret (whsec_*). Unique per webhook endpoint URL.
STRIPE_WEBHOOK_SECRET=
# CLI webhook signing secret (whsec_*) gotten from `stripe listen --forward-to localhost:3000/api/stripe/webhook`.
STRIPE_CLI_WEBHOOK_SECRET=
# Fixed PT VAT 23% tax rate id in your Stripe account (test/live variant).
STRIPE_TAX_RATE_PT_STANDARD=

# -----------------------------
# Operational toggles / security
# -----------------------------
# For dev seed scripts (0/1). Keep empty in production.
SEED_RESET=
# For smoke tests/dev routes (non-secret sample token).
SMOKE_TEST_KEY=
# Protects /ops actions (server-only).
OPS_PASSCODE=
# Cron shared secret (server-only).
CRON_SECRET=
# E2E test secret (server-only).
E2E_SECRET=
# Payment E2E test secret (server-only).
RUN_PAYMENT_E2E=
# Invoicing link secret (server-only). Long random string in staging and prod. A TTL policy, e.g. 7 days in staging while testing, 72 hours in prod emails.
INVOICING_LINK_SECRET=

# -----------------------------
# Database (server-only)
# -----------------------------
DATABASE_URL=

# -----------------------------
# Email / Resend
# -----------------------------
RESEND_API_KEY=
# "true" to send real emails in that environment; otherwise leave blank/false.
SEND_EMAILS=
EMAIL_FROM=noreply@amr-rentals.com
EMAIL_REPLY_TO=support@amr-rentals.com
EMAIL_ADMIN_TO=
ADMIN_TO=

# -----------------------------
# Public contact info (browser-visible)
# -----------------------------
NEXT_PUBLIC_SUPPORT_EMAIL=support@amr-rentals.com
NEXT_PUBLIC_WHATSAPP_NUMBER=+351934014611

# -----------------------------
# Company info (server-only unless UI needs it)
# If the UI reads any of these, mirror them with NEXT_PUBLIC_* variants.
# -----------------------------
COMPANY_NAME="Algarve Machinery Rental"
COMPANY_WEBSITE="https://amr-rentals.com"
SUPPORT_EMAIL="support@amr-rentals.com"
SUPPORT_PHONE="(+351) 934 014 611"
COMPANY_LEGAL_ADDRESS="Avenida da Liberdade, Centro Comercial Granjinhos, Loja 446, 4710-249, Braga, Portugal"
WAREHOUSE_ADDRESS="Barranco da Vaca, 8670-116, Aljezur, Portugal"
WAREHOUSE_HOURS="Mo-Fr 09:00-17:00"

# -----------------------------
# Maps / Geofence
# -----------------------------
ENABLE_GEOFENCE=true
MAPBOX_ACCESS_TOKEN=

# -----------------------------
# Vendus (server-only)
# -----------------------------
VENDUS_API_KEY=
VENDUS_URL=https://www.vendus.pt/ws/
# Optional; leave blank to auto-discover via API.
VENDUS_REGISTER_ID=
VENDUS_MODE=tests
INVOICE_DOC_TYPE=
INVOICE_ENABLED=

# -----------------------------
# Ads / SEO
# -----------------------------
# Production and staging URL in the Branch deploy context.
NEXT_PUBLIC_SITE_URL=http://localhost:3000 #or :8888
NEXT_PUBLIC_ENV=development

# -----------------------------
# Google Ads
# set for prod and blank for staging
# -----------------------------
#Production context:
NEXT_PUBLIC_GA4_ID= # G-XXXXXXXXXX
# optional; only if you’ll import Google Ads
NEXT_PUBLIC_GADS_ID= #AW-XXXXXXXXXX 

# -----------------------------
# Google Calendar (server-only; optional)
# -----------------------------
# GOOGLE_CALENDAR_SERVICE_ACCOUNT_EMAIL=
# GOOGLE_CALENDAR_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# GOOGLE_CALENDAR_ID=
# GOOGLE_CALENDAR_TIMEZONE=Europe/Lisbon

# -----------------------------
# Next.js dev tweaks (local only; keep out of cloud)
# -----------------------------
# NEXT_WEBPACK_HMR_SOCKET_HOST=localhost
# NEXT_WEBPACK_HMR_SOCKET_PORT=3000
